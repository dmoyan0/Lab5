syntax = "proto3";

package LAB5;
option go_package = "github.com/dmoyan0/Lab5";

service Broker {
    rpc SendAddress (CommandRequest) returns (CommandResponse);
}

message CommandRequest {
    int32 command = 1;
}

message CommandResponse {
    int32 address = 1;
}
